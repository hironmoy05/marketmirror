import React from 'react';
import {SendCoinContainer} from '../containers/sendCoinContainer';

export default function SendCoinScreen({ navigation }) {
    return (
        <SendCoinContainer navigation={navigation} />
    )
}