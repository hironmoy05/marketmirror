import React from 'react';
import { RegisterationContainer } from '../containers/registerationContainer';

export default function RegisterationScreen ({navigation}) {
    return <RegisterationContainer navigation={navigation} />;
}